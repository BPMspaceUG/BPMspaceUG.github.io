services:
  www:
    build: .
    hostname: training
    container_name: training
    restart: always
    working_dir: /var/www/html/
    networks:
      - training
      - caddy
    labels:
      caddy: training.test.bpmspace.net
      caddy.reverse_proxy: "{{upstreams http}}"

networks:
  training:
    driver: bridge
  caddy:
    external: true
